services:
  frontend:
    build:
      context: ./Frontend
    ports:
      - "80:80"
    depends_on:
      - backend

  backend:
    build:
      context: ./Backend
    ports:
      - "5000:5000"
    environment:
      - MONGO_URI=mongodb+srv://sanketyelugotla:sanket@speakx.abkna.mongodb.net/digiballot?retryWrites=true&w=majority&appName=speakx
      - JWT_SECRET=8abcc049fa12a8d797b7dea5326990d8039dd663de874a031ec48dd87759be61
      - PORT=5000
      - NODE_ENV=production

volumes:
  mongo-data:
